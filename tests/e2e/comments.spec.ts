import { test } from "@playwright/test";

test.describe("comments", () => {
  test.skip("comment form posts without client JavaScript when signed in", async () => {
    // requires local auth fixture / staging
  });
});
