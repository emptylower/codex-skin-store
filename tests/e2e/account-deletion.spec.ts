import { test } from "@playwright/test";

test.describe("account deletion", () => {
  test.skip("typed confirmation phrase deletes account and signs user out", async () => {
    // requires local auth fixture / staging
  });
});
