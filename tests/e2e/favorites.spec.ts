import { test } from "@playwright/test";

test.describe("favorites", () => {
  test.skip("signed-in user can favorite and see /me/favorites library", async () => {
    // requires local auth fixture / staging
  });
});
